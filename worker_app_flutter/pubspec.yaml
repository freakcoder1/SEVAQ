name: worker_app_flutter
description: SEVAQ Worker App - For service providers to manage bookings and earnings
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.0.5
  fl_chart: ^0.66.2
  google_fonts: ^6.1.0
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.4
  firebase_messaging: ^15.1.6
  flutter_local_notifications: ^18.0.1
  audioplayers: ^6.1.0
  vibration: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
